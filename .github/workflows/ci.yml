name: My First CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  verify-my-code:
    runs-on: ubuntu-latest
    steps:
      - name: Check out the code
        uses: actions/checkout@v4

      - name: Run a simulated test step
        run: |
          echo "Starting automated system tests..."
          echo "All tests passed successfully!"
